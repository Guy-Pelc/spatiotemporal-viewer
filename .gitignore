.DS_Store
__pycache__/
*.pyc

# macOS / Google Drive junk
Icon?
._*

# Google Drive doc pointers
*.gdoc
*.gsheet
*.gslides

# memsearch tool state
.memsearch/

# Local viewer copy — use the release tracked at repo root instead
/viewer/

# Raw source data — too large to track; the packed kidney/data/ bundle
# (built via export_kidney_data.py) is committed instead
/kidneys.h5ad
/kidneys_downsampled.h5ad
